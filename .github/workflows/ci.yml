name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go: ['1.21', '1.22', '1.23']

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}

      - name: Download dependencies
        run: go mod download

      - name: Run tests (with coverage)
        if: matrix.os == 'ubuntu-latest'
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Run tests
        if: matrix.os != 'ubuntu-latest'
        run: go test -v -race ./...

      - name: Run benchmarks
        if: matrix.os == 'ubuntu-latest' && matrix.go == '1.23'
        run: go test -bench=. -benchmem -benchtime=100ms ./...

      - name: Run fuzz tests (seed corpus only)
        if: matrix.os == 'ubuntu-latest' && matrix.go == '1.23'
        run: |
          go test -run='^$' -fuzz=FuzzCreateFile -fuzztime=10s ./...
          go test -run='^$' -fuzz=FuzzReadWrite -fuzztime=10s ./...
          go test -run='^$' -fuzz=FuzzSymlink -fuzztime=10s ./...

      - name: Upload coverage
        if: matrix.os == 'ubuntu-latest' && matrix.go == '1.23'
        uses: codecov/codecov-action@v4
        with:
          files: coverage.out
          fail_ci_if_error: false
